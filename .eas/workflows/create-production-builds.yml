name: Create Production Builds

on:
  push:
    branches: ["main"]

jobs:
  build_android:
    name: Build Android Preview APK
    type: build
    params:
      platform: android
      profile: preview
